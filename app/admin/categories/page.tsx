import type { Prisma } from "@prisma/client";
import Link from "next/link";
import { db } from "@/lib/db";

export default async function AdminCategoriesPage() { let categories: Prisma.CategoryGetPayload<{ include: { _count: { select: { products: true } } } }>[] = []; let error = ""; try { categories = await db.category.findMany({ include: { _count: { select: { products: true } } }, orderBy: { sortOrder: "asc" } }); } catch { error = "Database setup is required before categories can be managed."; } return <><header className="admin-page-heading"><p className="eyebrow">Catalogue management</p><h1>Categories</h1><p>Manage category details, images and search metadata.</p></header><div className="admin-toolbar"><span /><Link className="button button-primary" href="/admin/categories/new">New category <span>↗</span></Link></div>{error ? <div className="admin-alert">{error}</div> : <div className="admin-table-wrap"><table><thead><tr><th>Category</th><th>Slug</th><th>Products</th><th>Status</th><th><span className="sr-only">Action</span></th></tr></thead><tbody>{categories.map(category => <tr key={category.id}><td>{category.name}</td><td>{category.slug}</td><td>{category._count.products}</td><td><span className={`status status-${category.status.toLowerCase()}`}>{category.status.toLowerCase()}</span></td><td><Link href={`/admin/categories/${category.id}/edit`}>Edit</Link></td></tr>)}</tbody></table></div>}</>; }
